{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "env": ["PUBLIC_*", "PROD"],
      "inputs": ["$TURBO_DEFAULT$"],
      "persistent": true
    },
    "build": {
      "env": ["PUBLIC_*", "PROD"],
      "inputs": ["$TURBO_DEFAULT$"]
    },
    "start": {
      "env": ["PUBLIC_*", "PROD"],
      "persistent": true
    }
  }
}
